-- filter.lua - Pandoc Lua filter applied to every page.
-- Scaffold: define filter functions here to transform AST nodes.
-- See https://pandoc.org/lua-filters.html for the filter API.
--
-- Example:
--   function Div(el)
--     if el.classes:includes('note') then
--       return pandoc.Div(el.content, pandoc.Attr('', {'note'}))
--     end
--   end

return {}
